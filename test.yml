env:
  mocha: true
globals:
  assert: true
  expect: true
  mock: true
  should: true
  spy: true
  stub: true
rules:
  max-len:
    - error
    - ignorePattern: ^\s*(it|describe)\(
      ignoreRegExpLiterals: true
  no-magic-numbers: off
  no-unused-expressions: off
