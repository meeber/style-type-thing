{
  "name": "eslint-config-tt",
  "version": "9.2.0",
  "description": "style guide and ESLint config",
  "author": "Grant Snodgrass <meeber@gametypething.com>",
  "license": "MIT",
  "repository": "meeber/style-type-thing",
  "engines": {
    "node": ">=4.0.0"
  },
  "main": "base.yml",
  "scripts": {
    "postversion": "git checkout dev && git merge master && git push && git push --tags && npm publish",
    "preversion": "git checkout master && git merge dev && npm test",
    "release": "npm version -m 'Finalize v%s'",
    "test": "echo '// just validating eslint config' | eslint --stdin"
  },
  "devDependencies": {
    "eslint": "^4.7.2"
  },
  "peerDependencies": {
    "eslint": "^4.7.2"
  }
}
